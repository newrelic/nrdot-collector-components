# Receivers

This directory is reserved for future OpenTelemetry Collector receiver components. Currently contains only minimal placeholder components but ready for receiver implementations as needed.
